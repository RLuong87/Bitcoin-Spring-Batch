CREATE TABLE bitcoin (
unix_timestamp VARCHAR(20),
datetime VARCHAR(20),
open VARCHAR(20),
high VARCHAR(20),
low VARCHAR(20),
close VARCHAR(20),
volume_btc VARCHAR(20),
volume_currency VARCHAR(20),
weighted_price VARCHAR(20)
);